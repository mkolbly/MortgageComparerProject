package com.kolbly.android.graphics;

/**
 * Listener for when a user is done editing an EditTextEx object
 * 
 * @author mkolbly
 */
public interface EditTextExListener
{
	/**
	 * Callback for when user is done editing an EditTextEx
	 * 
	 * @param view EditText object of interest
	 */
	void onDoneEditing(EditTextEx view); 
}
